# oops Testing.
"""
global base
global bal
#a=input("Enter amount withdraw: ")
#b=input("Enter amount deposite: ")

def withd(self,bal ):
    #if a < bal:
     if dep > base :
        print("cannot withdraw amount > 10000")
     else:
         bal = bal - withd
         return




def dep(b):
  bal= bal+b
  return
#print(bal)


withd(100)
dep(2000)

class Account:
    def __init__(self): #construtor method
        def showbal(self):
            print(globals())
        # class access using syntax:
        #<object>.<method_name>()
        #<object>.<variable> [=<value>]

class Me:
    def __init__(self,name,age,sex):
        self.name = name
        self.age = age
        self.sex = sex
i =Me("Prasad", "28","Male")

print(i.name)
print(i.age)
print(i.sex)



class Employee:
    def __init__(self, name, login, unit):
        self.name = "prasad"
        self.login = "prasad.keluskar"
        self.unit = "QA"

def showdetails(self):
    print(f"{self.name} log in with { self.login} and is unit is{self.unit}")

e = Employee()
e.name()        """

#decoraters - syntactic sugar
#@
import pythonlanngutil.Overload
import Overload.signature

class Person():
    @Overload
    @Signature # define parameter list
    def inint(self , name):
        self.name = "xxx"

p = Person()
p.name()